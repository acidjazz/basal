extends ../extends/layout

block content
  .page.margined.structures
    a.ctap(href='/structures/new') New Structure
    .clear
    .content
      .listing.structures
        include ../partial/listing

    script.
      Page = 'structures';
      Global.init = 'Structures';
