extends ../extends/layout

block content

  .page.margined.entries
    a.ctap(href='/entries/new') New Entry
    .clear
    .content
      .listing.entries
        - var buttons = [];
        include ../partial/listing

    script.
      Page = 'entries';
      Global.init = 'Entries';
