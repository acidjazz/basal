extends ../extends/layout

block content
  .page.margined.clients
    a.ctap(href='/clients/new') New Client
    .clear
    .content
      .listing

    script.
      Page = 'clients';
      Global.init = 'Clients';
