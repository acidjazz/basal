extends ../extends/layout

block content
  .page.dashboard 
    .title dashboard
    .collections
      a.collection.users(href='/users')
        .title Users
        .stat
          .copy Total
          .value(data-value='users.paginate.total') ?
      a.collection.clients(href='/clients')
        .title Clients
        .stat
          .copy Total
          .value(data-value='clients.paginate.total') ?

    script.
      Dashboard.i();
