machine:
  timezone:
    America/Los_Angeles
  ruby:
    version: 2.4.0
  services:
    - docker

compile:
  override:
    - rake

dependencies:
  override:
    - gem install rake

database:
  override:
    - sleep 1

test:
  override:
    - sleep 1

deployment:
  override:
    - sleep 1
